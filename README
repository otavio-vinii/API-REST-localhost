# 🚀 API de Usuários (Node.js)

Uma API REST simples para listar e criar usuários, mantendo os dados em memória. Este projeto é ideal para estudos introdutórios de **Node.js** e **Express**.

> **Aviso:** Como os dados são salvos em variáveis locais (memória), reiniciar o servidor limpará a lista de usuários cadastrados.

## 🛠️ Tecnologias Utilizadas

* [Node.js](https://nodejs.org/)
* [Express](https://expressjs.com/)
* [Nodemon](https://nodemon.io/) (Dev dependency)

## 📦 Instalação e Configuração

### Pré-requisitos
Antes de começar, você precisa ter o [Git](https://git-scm.com) e o [Node.js](https://nodejs.org/en/) instalados em sua máquina.

### Passo a passo

1. Clone este repositório:
   ```bash
   git clone [https://github.com/seu-usuario/nome-do-repo.git](https://github.com/seu-usuario/nome-do-repo.git)

```

2. Acesse a pasta do projeto:
```bash
cd nome-do-repo

```


3. Instale as dependências:
```bash
npm install

```


> **Nota:** O Nodemon já está configurado no `package.json`, então ele será instalado automaticamente.



## ▶️ Como Rodar

### Modo Desenvolvimento

Utiliza o Nodemon para reiniciar o servidor automaticamente ao salvar alterações:

```bash
npm run dev

```

### Modo Produção (Padrão)

```bash
node index.js

```

O servidor estará rodando em: `http://localhost:3000`

---

## 🔗 Endpoints da API

| Método | Rota | Descrição |
| --- | --- | --- |
| **GET** | `/usuario` | Retorna a lista de usuários |
| **POST** | `/usuario` | Cria um novo usuário |

### 📝 Exemplos de Requisição

#### 1. Criar Usuário (POST)

**Rota:** `POST /usuario`

**Body (JSON):**

```json
{
  "name": "Maria Silva",
  "age": 28
}

```

#### 2. Listar Usuários (GET)

**Rota:** `GET /usuario`

**Response (Exemplo):**

```json
[
  {
    "id": 1,
    "name": "Maria Silva",
    "age": 28
  }
]

```

---

## 🧪 Como Testar

Como esta API não possui Front-end, você deve utilizar ferramentas para testar as rotas:

* **[Insomnia](https://insomnia.rest/download)** (Recomendado)
* **[Postman](https://www.postman.com/)**
* **VS Code (Extensão Thunder Client)**

### Teste rápido via Terminal (cURL)

Se você usa Linux ou Mac, pode testar rapidamente colando isso no terminal:

```bash
curl -X POST http://localhost:3000/usuario -H "Content-Type: application/json" -d '{"name": "Teste", "age": 99}'

```